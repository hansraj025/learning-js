
const user = 'Vegeta';
const excitement = 90001;  // Over 9000
let> powerUp = 'level 1';

if (excitement > 9000) {
    powerUp = 'level 2';
    console.log(`${user}'s power is ${powerUp} now`);
} else {
    console.log(`${user}'s power is ${powerUp} now`);
}

